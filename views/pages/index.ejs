<!DOCTYPE html>
<html lang="en" class="bg-transparent">
  <head>
    <%- include('../partials/head'); %>
  </head>
  <body class="container bg-transparent">
    <!--<iframe class="absolute top-0 bottom-0 left-0 right-0 h-screen w-screen" src="https://recipesai.co/" frameborder="0"></iframe>-->
    <header><%- include('../partials/header'); %></header>
    <main>
      <div
        class="relative z-10"
        x-data="{ isOpen: false, closeModal: function() { isOpen = false; setTimeout(() => { window.parent.postMessage('close', '*'); }, 300); } }"
        x-init="setTimeout(() => isOpen = true, 10)"
        aria-labelledby="modal-title"
        role="dialog"
        aria-modal="true"
      >
        <div
          x-show="isOpen"
          x-transition:enter="ease-out duration-300"
          x-transition:enter-start="opacity-0"
          x-transition:enter-end="opacity-100"
          x-transition:leave="ease-in duration-200"
          x-transition:leave-start="opacity-100"
          x-transition:leave-end="opacity-0"
          class="fixed inset-0 bg-slate-500 dark:bg-slate-900 bg-opacity-75 dark:bg-opacity-75 transition-opacity"
        ></div>

        <div class="fixed inset-0 z-10 overflow-y-auto">
          <div class="flex h-full min-h-full items-center justify-center p-4 text-center sm:items-center sm:p-0">
            <div
              x-show="isOpen"
              x-transition:enter="ease-out duration-300"
              x-transition:enter-start="opacity-0 translate-y-4 sm:translate-y-0 sm:scale-95"
              x-transition:enter-end="opacity-100 translate-y-0 sm:scale-100"
              x-transition:leave="ease-in duration-200"
              x-transition:leave-start="opacity-100 translate-y-0 sm:scale-100"
              x-transition:leave-end="opacity-0 translate-y-4 sm:translate-y-0 sm:scale-95"
              class="relative my-8 flex w-full max-w-lg transform flex-col overflow-hidden rounded-2xl bg-white text-left shadow-xl transition-all dark:bg-slate-600"
            >
              <form action="/createRecipe" method="post">
                <div class="-h-full bg-white px-4 pb-4 pt-5 text-black dark:bg-slate-700 dark:text-white sm:p-6">
                  <h2 class="mb-2 text-2xl font-semibold tracking-tight">Generate a new recipe</h2>
                  <h3 class="mb-2">Type your recipe idea below!</h3>
                  <div>
                    <label for="email" class="block text-sm font-medium"></label>
                    <div class="mt-1">
                      <% if (typeof id !== 'undefined') { %>
                      <input
                        type="text"
                        name="food"
                        id="text"
                        placeholder="Spaghetti with meatballs"
                        value="<%= id %>"
                        class="pl-4 block w-full rounded-full border-slate-300 shadow-sm focus:border-slate-400 focus:ring-slate-400 dark:border-slate-500 dark:bg-slate-600 dark:placeholder-slate-400"
                      />
                      <% } else { %>
                      <input
                        type="text"
                        name="food"
                        id="text"
                        placeholder="Spaghetti with meatballs"
                        class="pl-4 block w-full rounded-full border-slate-300 shadow-sm focus:border-slate-400 focus:ring-slate-400 dark:border-slate-500 dark:bg-slate-600 dark:placeholder-slate-400"
                      />
                      <% } %>
                    </div>
                  </div>
                </div>
                <div class="flex flex-col-reverse bg-slate-50 px-4 py-4 dark:bg-slate-600 sm:flex-row-reverse sm:px-6 sm:py-3">
                  <button
                    type="submit"
                    class="mt-3 inline-flex w-full items-center justify-center rounded-full border border-blue-400 bg-blue-600 px-4 py-2 text-base font-medium text-white shadow-sm transition-colors hover:border-blue-500 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 dark:ring-offset-slate-600 sm:ml-3 sm:mt-0 sm:w-auto sm:text-sm"
                  >
                    Generate
                  </button>
                  <button
                    type="button"
                    x-on:click="isOpen = false"
                    class="inline-flex w-full items-center justify-center rounded-full border border-slate-300 bg-white px-4 py-2 text-base font-medium text-slate-700 shadow-sm transition-colors hover:bg-slate-50 focus:outline-none focus:ring-2 focus:ring-slate-400 focus:ring-offset-2 dark:border-slate-400 dark:bg-slate-500 dark:text-white dark:ring-offset-slate-600 dark:hover:border-slate-500 dark:hover:bg-slate-700 sm:ml-3 sm:w-auto sm:text-sm"
                  >
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="mr-2 h-6 w-6">
                      <path stroke-linecap="round" stroke-linejoin="round" d="M6.75 15.75L3 12m0 0l3.75-3.75M3 12h18" />
                    </svg>
                    Cancel
                  </button>
                </div>
              </form>
            </div>
          </div>
        </div>
      </div>
    </main>
    <footer><%- include('../partials/footer'); %></footer>
  </body>
</html>
